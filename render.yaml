services:
  - type: web
    name: nlp-backend-api
    env: python
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.6
    healthCheckPath: /api/health
    plan: free

  - type: web
    name: nlp-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
